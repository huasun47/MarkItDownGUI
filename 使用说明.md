# MarkItDown GUI - 使用说明

## 系统要求
- Python 3.10
- Windows 11
- uv 包管理器

## 快速启动

### 方法1：使用uv直接运行
```bash
uv run python gui_app.py
```

### 方法2：使用启动脚本
```bash
./run.bat
```

### 方法3：激活虚拟环境
```bash
uv shell
python gui_app.py
```

## 功能特性

### 支持的文件格式
- **办公文档**: PDF (.pdf), Word (.docx), PowerPoint (.pptx), Excel (.xlsx)
- **多媒体**: 音频 (.mp3, .wav)
- **网络资源**: 网页URL, Wikipedia页面, YouTube视频字幕, Bing搜索结果, RSS订阅源
- **数据与电子书**: 电子书 (.epub), 数据文件 (.csv, .json, .xml), 压缩包 (.zip)

### 主要功能
- 🖱️ **拖拽操作**: 直接将文件拖拽到应用程序中
- 📂 **批量处理**: 一次性处理多个文件
- 🌐 **网页转换**: 支持百度、知乎、微信公众号等网站
- 🎵 **音频转录**: 音频文件转文字
- 🛡️ **本地隐私**: 所有转换在本地进行
- ⚙️ **灵活输出**: 可选择保存位置

## 常见问题

### Q: 关于ffmpeg警告
A: 这是正常的警告，只在处理音频文件时需要ffmpeg。其他功能不受影响。

### Q: 如何添加文件
A: 
- 点击"添加文件"按钮选择文件
- 直接将文件拖拽到文件列表中

### Q: 如何批量处理
A: 添加多个文件后，点击"开始转换"按钮，程序会自动排队处理所有文件。

### Q: 输出文件保存在哪里
A: 
- 默认保存在原文件同目录
- 可以在设置中选择统一输出文件夹

## 技术信息

### Python版本
- 当前使用: Python 3.10.20
- 配置文件: pyproject.toml

### 主要依赖
- PyQt6 (GUI框架)
- MarkItDown (文档转换核心)
- BeautifulSoup4 (网页解析)
- Pandas (数据处理)
- 其他格式处理库

### 项目结构
```
MarkItDownGUI/
├── gui_app.py          # 主应用程序
├── markitdown/         # MarkItDown核心模块
├── pyproject.toml      # 项目配置
├── requirements.txt    # 依赖列表
├── run.bat            # 启动脚本
└── test_setup.py      # 测试脚本
```

## 故障排除

### 如果应用程序无法启动
1. 确保Python 3.10已安装
2. 运行 `uv sync --python 3.10` 重新安装依赖
3. 运行 `uv run python test_setup.py` 检查环境

### 如果转换失败
1. 检查文件格式是否支持
2. 确保文件没有损坏
3. 检查网络连接（网页转换需要）

## 获取帮助
如果遇到问题，请检查：
1. Python版本是否为3.10
2. 所有依赖是否正确安装
3. 文件路径是否正确

---
*本项目基于微软的 MarkItDown 库构建*
